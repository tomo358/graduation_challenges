<%= form_with(model: musical_instrument, local: true) do |form| %>
  <% if musical_instrument.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(musical_instrument.errors.count, "error") %> prohibited this musical_instrument from being saved:</h2>

      <ul>
      <% musical_instrument.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :musical_instrument_name %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :musical_instrument_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
